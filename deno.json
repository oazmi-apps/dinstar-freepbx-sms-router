{
	"exclude": [
		"./dist/",
		"./docs/",
		"./npm/"
	],
	"fmt": {
		"useTabs": true,
		"semiColons": false,
		"singleQuote": false,
		"lineWidth": 800,
		"proseWrap": "preserve",
		"include": [
			"./cmd/",
			"./pkg/",
			"./src/",
			"./utilities/",
			"./test/",
			"./**/*.md",
			"./**/*.json",
			"./**/*.yml",
			"./**/*.yaml"
		]
	},
	"compilerOptions": {
		"types": [],
		"lib": ["esnext", "dom", "node"],
		"strict": true
	},
	"tasks": {
		"build": {
			"description": "create an uploadable and installable freepbx addon/module package.",
			"command": "7z a -ttar \"./dist/dinstarsms.tar\" \"./*.php\" \"./module.xml\""
		},
		"test-register": {
			"description": "register the binary (placed under your distribution folder) as a startup daemon on linux, or a startup service on windows.",
			"command": "./dist/cli.exe --register"
		},
		"test-unregister": {
			"description": "unregister the binary from booting up as a daemon/service during startup.",
			"command": "./dist/cli.exe --unregister"
		},
		"test-webhook": {
			"description": "todo: test the webhook with freepbx.",
			"command": "todo: how will this even get tested?"
		}
	}
}
